routes:
  /subscribe/: members/subscribe
  /signup/: members/signup
  /signin/: members/signin
  /account/: members/account
  /upgrade/: members/upgrade

collections:
  /:
    permalink: /{slug}/
    template: index

# collections:
#   /blog/:
#     permalink: /blog/{slug}/
#     template: index
#     filter: tag:-blog
#     data: tag.blog

# Using Private tags
# /posts/:
#   permalink: /posts/{slug}/
#   template: index
#   filter: tag:hash-post
#   filter: tag:[iphone,ipad,mac]

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
